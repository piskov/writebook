<% content_for(:title) { "Настройки: #{@book.title}" } %>

<% content_for :header do %>
  <nav>
    <%= link_to book_slug_path(@book), class: "btn flex-item-justify-start" do %>
      <%= image_tag "arrow-left.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Отменить и вернуться</span>
    <% end %>
  </nav>
<% end %>

<%= render "books/form", book: @book, users: @users, creating_user: nil %>

<% content_for :footer do %>
  <nav class="flex align-end justify-center pad">
    <%= button_to book_path(@book), method: :delete, class: "btn btn--negative",
          data: { turbo_confirm: "Удалить эту книгу? Это действие нельзя отменить." } do %>
      <%= image_tag "trash.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Удалить <%= @book.title %></span>
    <% end %>
    <button type="submit" form="book-editor", class="new-book-btn btn btn--reversed center txt-medium--responsive" aria-label="Сохранить изменения" title="Сохранить изменения">
      <%= image_tag "check.svg", aria: { hidden: true }, size: 24 %>
    </button>
  </nav>
<% end %>
